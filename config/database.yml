# # SQLite version 3.x
# #   gem install sqlite3
# #
# #   Ensure the SQLite 3 gem is defined in your Gemfile
# #   gem 'sqlite3'
# #
# default: &default
#   adapter: sqlite3
#   pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
#   timeout: 5000

# development:
#   <<: *default
#   database: db/development.sqlite3

# # Warning: The database defined as "test" will be erased and
# # re-generated from your development database when you run "rake".
# # Do not set this db to the same as development or production.
# test:
#   <<: *default
#   database: db/test.sqlite3

# production:
#   <<: *default
#   database: db/production.sqlite3

# USING POSTGRES 

default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  encoding: unicode
  pool: 10
  # timeout: 5000

development:
  <<: *default
  database: psql_bf_dev
  username: max         # -U for Postgres
  password: 

test:
  adapter: postgresql
  database: psql_bf_test
  username: max         # -U for Postgres
  password:

production:
  <<: *default
  database: psql_bf_dev
  username: max
  password:

  # database: pg_production